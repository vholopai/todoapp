package utils;

import java.io.File;

public class Constants {
    public static final String VIEWPATH = "./" + File.separator + "static" + File.separator;
    public static final String TODOPATH = "./" + File.separator + "data" + File.separator + "todo" + File.separator;
    public static final String DONEPATH = "./" + File.separator + "data" + File.separator + "done" + File.separator;    
    public static final String REMOVEDPATH = "./" + File.separator + "data" + File.separator + "removed" + File.separator;    
    
}
